import React from 'react';
import DefaultBoard from '../Board/DefaultBoard';

const Body = () => {
  return (
    <div>
      <DefaultBoard />
    </div>
  );
};

Body.propTypes = {};

export default Body;
